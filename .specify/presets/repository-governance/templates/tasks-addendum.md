## Repository Task Constraints

- `tasks.md` is a derived, dependency-ordered implementation checklist, not external task or claim state.
- Every task must remain inside repository-defined scope and the exact accepted specification binding.
- Do not create external issues for `Txxx`, create or switch branches, or encode assignment, review, release, or
  completion state in this file unless repository guidance explicitly delegates that operation.
- Include the exact repository validation commands selected by the router and the accepted plan.
