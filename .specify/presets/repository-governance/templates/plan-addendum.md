## Repository Planning Constraints

- Resolve the repository router and current source authorities before choosing an implementation.
- Replace every generic directory example with the actual application, contract, documentation, or design boundary
  selected by repository guidance.
- Bind this plan to the exact accepted specification snapshot and list the risk-based validation required for every
  changed boundary.
- Do not expand repository-defined scope or make a product, security, architecture, ownership, or priority decision
  implicitly.
