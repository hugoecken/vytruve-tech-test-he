---
description: "Disabled because repository guidance exclusively owns execution items"
---

# Disabled: `taskstoissues`

Stop without reading remote credentials and without creating or modifying any issue, Project item, branch, or file.

This integration deliberately keeps `tasks.md` as a derived checklist inside one feature directory. It must never be
converted into external issues or treated as shared execution state. Use the intake and acquisition flow selected by
the repository guidance for any new execution item.
