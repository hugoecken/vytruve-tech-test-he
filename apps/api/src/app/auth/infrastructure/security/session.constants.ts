/** Browser cookie carrying the private JWT session. */
export const SESSION_COOKIE_NAME = 'vytruve_session';

/** Accepted session lifetime in seconds. */
export const SESSION_LIFETIME_SECONDS = 30 * 60;
